<li class="{{unbound view.reportType}} {{unbound view.reportStatus}}">
  <div class="exportInfo">
    <a href="{{unbound view.reportLink}}">

      <h5>
        {{view.reportTypeString}} - <span class="exportFileName">{{view.reportFilename}}</span>
        <span class="submissionsDate">
          {{#if view.content.lastCollectionOnly}}
          {{t _recent_submissions}}
          {{else}}
          {{#if view.startDate}}{{t _from}} {{view.startDate}} {{/if}}{{#if view.endDate}}{{t _to}} {{view.endDate}}{{/if}}
          {{/if}}
        </span>
      </h5>

    </a>
    <span class="surveyPath">{{view.surveyPath}}</span>
  </div>
  <div class="exportStatus">
    <span class="loading">{{t _generating}}</span>
    <span class="exportDate">{{#if view.lastUpdateDateTime}}{{view.lastUpdateDateTime}}{{/if}}</span>
  </div>
</li>
